# coding=utf-8
{% if authentication_type == 'jwt' %}
# Secret for JWT
JWT_SECRET = '{{jwt_secret}}'
{% endif %}
# Database URI, example: mysql://username:password@server/db?charset=utf8mb4
# SQLALCHEMY_DATABASE_URI = ''

# Where to put the models generated from database tables, default is: '{{project_name}}/models'
# table2model_dest = ''
